export function bootstrap(_app: unknown, _props: unknown) {
  return Promise.resolve();
}

export function mount(_app: unknown, _props: unknown) {
  return Promise.resolve();
}

export function unmount(_app: unknown, _props: unknown) {
  return Promise.resolve();
}
